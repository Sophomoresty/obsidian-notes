我根据题目大概罗列了下重点, 下面的内容
有些地方 可能有错误, 希望你改正,
有些地方 并不全, 希望你补全,
对于一维优化和二维优化的计算方法, 你可以举几个简单的计算例子, 帮我掌握, 然后我们之前有过的例子, 直接用就行了, 比如黄金分割, 惩罚函数的对数内点和外点法,
部分内容已经进行了归纳,
然后整体内容我不知道该如何编排, 麻烦你编排一下



分位数的概念, 怎么用 matlab 求, 三均值的概念和公式, 如何用 MATLAB 求

一元线性回归要考代码的书写, 也就是要知道系数 ab 如何求, lxx 和 lyy 怎么求, 以及 TSS, RSS, ESS 如何求, 会求之后还要掌握代码的书写.
还有回归平方和

还要知道 TSS, RSS, ESS 之间的关系

回归诊断包括哪些内容?

大数据的特征


常用的回归方程的显著性检验方法有哪些?
F 检验, T 检验, 可决系数 $R^2$ 检验
可决系数的范围在0到1之间, 越接近1, 方程越显著, 越接近0, 方程越不显著.

要知道峰度和偏度的概念, 特别是峰度

峰度是衡量数据尾部分散性的指标, 总体分布是正太分布时, 峰度近似为0

偏度是衡量分布的非对称程度或偏斜成都的特征, 总体分布是正态分布时, 偏度近似为0


总体分布的正态性检验有哪些? JB 检验, KS 检验, Lilliefors 检验(改进 KS 检验)

要知道检验的原理, JB 检验是利用正太分布的偏度 sk 和 峰度 ku 进行检验

ks 和 Lilliefores 都是通过样本经验分布函数与给定的分布函数进行比较进行检验.

ks 用于正态性检验只能做标准正态检验

一维正态分布也可以用 Q-Q 图检验方法

多维正态分布一般用 Q-Q 图检验方法


Lillefores 只能做一般的正态性检验.

数据变换的方法有哪些?什么时候用?

统一趋势和无量纲化, 压缩变换模糊化, Box-Cox 变换 (当时间数据序列在左 (右) 侧有长尾巴或很不对称时使用)

pdf 和 cdf 是什么意思? 经验分布函数和概率密度分布函数的区别是什么?

为了应对简答题, 要知道数据分析处理问题的具体流程.

要知道数据的分类, 
1、按照所采用的计量测度不通，可以将统计数据分为分类数据、顺序数据、数值型数据
2、按照统计数据的收集方法，将其分为观测数据和实验数据
3、按照描述现象于时间的关系，可以将统计数据分为横截面数据和时间序列数据
还可以分为一手数据和二手数据

优化设计方面

要知道优化设计的具体流程, 数据分析和优化设计的区别和联系?

优化设计三要素是什么?

一维优化要掌握外推法, 黄金分割法, 二次插值法,

二维优化要掌握, 梯度法, 牛顿法, 共轭方向和鲍威尔法,可行方向法,惩罚函数法的内点法和外点法

需要掌握概念, 要掌握手算, 知道计算步骤, 找对应的题进行总结, 后续要把书上的每个题做一遍



### 上下截断点与 IQR
考 `上下截断点的代码表达`, 因此最好理解上下截断点和四分位极差的概念和公式, 以及 matlab 如何求四分位极差
#### 概念

在统计学中，常用一种基于 **四分位极差 (Interquartile Range, IQR)** 的准则来识别潜在的异常值。

IQR 本身是一个稳健的离散程度度量，其定义为上四分位数 ($Q_3$) 与下四分位数 ($Q_1$) 之差，即 $IQR = Q_3 - Q_1$，它代表了数据中间50%的散布范围。在 MATLAB 中，可以通过 `iqr(A)` 命令计算得到。

基于 IQR，我们可以定义出判断异常值的“围栏”，即**上下截断点**。任何超出这个范围的数据点，都被视为潜在的异常值。

#### 公式及代码
**四分位极差**的公式: $IQR=Q_3-Q_1$

代码表示:
```matlab
% 求数据A的四分位极差
R = iqr(A)
```

**上截断点**的公式为 $Q_3 + 1.5 \times IQR$

代码表示:
```matlab
% 求数据A的上截断点
SJ = prctile(B,[75]) + 1.5 * iqr(A)
```

**下截断点**的公式为 $Q_1 - 1.5 \times IQR$。

代码表示
```matlab
% 求数据A的下截断点
XJ = prctile(B,[25]) - 1.5 * iqr(A)
```


### 异常点与强影响点的诊断统计量

**异常点 (Outlier)**，它特指在 Y 方向上严重偏离模型预测值的点，其残差很大。

> 统计量为: 残差, 皮尔森残差, 学生化残差


**强影响点 (Influential Point)**，指那些一旦移除就会对回归模型产生巨大改变的点，它通常需要同时具备高杠杆和高残差两个特征.

>  统计量为: 杠杆值, Cook 距离, CovRatio 统计量, Dffits 攻击力, Dfbeta 统计量
### 计算题