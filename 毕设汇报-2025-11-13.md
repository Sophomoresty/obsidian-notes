# 本周阅读文献

本周主要完成了 HarmonyOS PLC 仿真系统相关文献的系统性阅读，范围包括英文论文 2 篇、中文论文 4 篇，重点聚焦以下三个方向：

1. **现代 PLC 仿真系统的架构设计**
2. **SoftPLC 的实现方式与指令执行机制**
3. **PLC 控制流程、I/O 模型对仿真系统的启发**


# 英文文献阅读及收获

## Design of an IoT-PLC: A containerized programmable logical controller for the Industry 4.0

文章介绍了现代 PLC 的模块化、虚拟化趋势。
文章结构包括数据接口层、数据过滤层、运行时控制器、HMI 等多个独立模块。
### 收获
* PLC 仿真系统应拆分为独立模块（执行引擎、数据区、界面），这与 HarmonyOS 的 Ability/Service 机制一致。
* 其“虚拟设备模型”思想适合作为后续 I/O 仿真的数据抽象方案。
* 容器化和模块隔离的设计理念为系统的**可扩展性**提供方法论依据。
 
## Design of a PLC System Simulator and Application to Teaching Programmable Logic Controller Course Online

该论文给出了完整 PLC 仿真软件的结构：
Soft PLC 运行引擎、梯形图编辑器、变量监控界面、3D I/O 模拟器等。
![[毕设汇报-2025-11-13.md_Attachments/毕设汇报-2025-11-13-20251113095613128.png|400]]
![[毕设汇报-2025-11-13.md_Attachments/毕设汇报-2025-11-13-20251113095550692.png|400]]
![[毕设汇报-2025-11-13.md_Attachments/毕设汇报-2025-11-13-20251113095651538.png|400]]

### 收获
* UI 与执行引擎需要解耦：引擎使用底层语言（C++），UI 使用上层框架（适合 HarmonyOS 的 ArkUI）。
* 指令集不必完整实现，论文中也是选择了“最常用指令子集”，符合指导老师提出的“重点在设计，可扩展性为主”。
* 输出状态实时高亮、变量实时刷新，是后期调试界面的关键点。

# 中文文献阅读及收获

## 软PLC技术研究及实现

SoftPLC 的两个核心：
> 这里要特别说明目标代码和虚拟机两个概念。目标代码
> 一者之斯上斯者码，采用连续的二进制代码的形式，具有完整的指令系统。这些代码在编辑环境中生成，用来描述用户的控制功能，由虚拟机解释执行。目标代码的执行不依赖于虚拟机的运行坏境，当虚拟机运行在不同的操作系统平台上时，不需要修改编逢后的目棕代码
* 编辑环境（程序 → 中间代码）
* 运行环境（虚拟机执行指令）

以及完整的PLC仿真软件结构的构成

### 收获
* 需要设计PLC 中间指令格式（IR），再由 C++ 引擎解释执行。
* 扫描周期（scan cycle）是必做的核心功能：
  读取输入 → 执行逻辑 → 刷新输出。

## 基于 PLC 的工业控制系统设计

论文从工业生产线出发，强调 PLC 在实际系统中的作用以及 I/O 映射方式。
### 收获
* HarmonyOS 仿真软件需要支持虚拟输入区（X）、输出区（Y）、内部标志区（M）。
* I/O 只能通过统一的image table管理，便于后续扩展外部仿真插件。


# 本周初步成果：HarmonyOS PLC 仿真系统的架构雏形

确认了系统架构方向：
## 1. 底层模块（C++）

* **指令解析器（ST/STL）**
* **PLC 执行引擎（虚拟机，支持扫描周期）**
* **数据区管理：X、Y、M、T/C**

## 2. 上层模块（ArkTS + ArkUI）

* **梯形图编辑界面（基础版）**
* **变量监控界面**
* **虚拟 I/O 面板**

## 3. 设计原则

* 指令集精简实现，可扩展
* 架构分层清晰、UI 与逻辑完全解耦
* 后期可以接入外部传感器仿真模块（符合 IoT-PLC 的虚拟设备理念）


# 下周计划

1. 汇总指令集实现范围（确定最小可行指令子集）
2. 绘制详细的系统架构图（模块交互、数据流）
3. 开始设计 PLC 执行引擎的数据结构（变量区、程序区）

