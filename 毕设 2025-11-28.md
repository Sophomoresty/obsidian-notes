本周2025-11-23:2025-11-28工作是
- 确定`仿真指令集`
- 阅读`开源项目 OpenPLC 的代码`
## 1.仿真指令集

结合 IEC 61131-3 标准中的常用指令类别以及 OpenPLC 当前实现情况，初步确定的最小实现集合

- `1-逻辑与线圈类指令`: 实现基本的**顺序控制**
	- 触点类
		- `LD`加载
		- `LDN`取反加载
	- 逻辑运算
		- `AND`
		- `ANDN`
		- `OR`
		- `ORN`
		- `NOT`
	- 线圈类
		- `OUT`输出线圈
		- `SET`置位
		- `RST`复位
- `2-比较与条件控制类指令`: 实现简单的分支/跳转指令
	* `EQ`等于
	* `NE`不等于
	* `GT`大于
	* `GE`大于等于
	* `LT`小于
	* `LE`小于等于
- `3-数据传送与算术类指令`: 实现基本数值处理
	* `MOV`
	* `ADD`
	* `SUB`
	* `MUL`
	* `DIV`
- `4-定时器与计数器类指令`
	* `TON`通电延时-定时器
	* `TOF`延时开启
	* `CTU`向上计数-计数器
	* 预留 `EN`、`DN`、`ACC` 等状态量字段
	- 变量及存储区域规划
		* 布尔 I/O
			* `%IX` `%QX`
		* 字/整型 I/O
			* `%IW`、`%QW`
		* 内部软元件
			* `%M` 
			* 存储内部继电器/中间变量
		* 预留定时器/计数器专用区
			* 如 `%T`、`%C`
			* 实例化功能块

## 2.阅读OpenPLC 开源项目的代码

#### 1)扫描周期模型
- 文件
	- `webserver/core/main.cpp`
- 总结
	* `config_run__(__tick++)` 作为用户控制程序的统一执行入口
	* 用 `sleep_until(&timer_start, common_ticktime__)` `控制扫描周期(默认50s)`
	* 用 `pthread_mutex_lock` / `unlock` 对 I/O 缓冲访问加锁, 提供同步互斥机制, 实现多线程

#### 2)数据区与 I/O 映像区管理
* 文件
	* `webserver/core/ladder.h`
* 总结
	* `bool_input / bool_output`、`int_input / int_output` 等数组的组织方式, 将 `%IX0.0`、`%QX0.0` 等地址映射为按字节+位访问
	*  `int_memory[]`、`dint_memory[]`构建内部软元件、寄存器区；
	* 变量与 I/O 映像区之间通过 `glueVars()` 完成绑定

#### 3)IEC 类型与标准功能块实现
- 文件
	- `webserver/core/lib/iec_types.h`
	- `webserver/core/lib/iec_std_FB.h`
- 总结
	* `IEC_BOOL`、`IEC_UINT`、`IEC_REAL` 定义基础 类型指令
	* `TON`、`CTU`用C的结构体定义和函数实现