某计算机采用页式虚拟存储管理方式，按字节编址。CPU 进行存储访问的过程如题 44 图所示。
![[2018-44-15分-虚拟存储.md_Attachments/2018-44-15分-虚拟存储-20251217140726509.png|500]]
**(1) 主存物理地址占多少位？**
通过图中cache缺失的流程发现, 采用二级页表, VPN高10位是页目录号, 低10是页表项号, 依此得到PPN.
由图知道最后访问cache的物理地址是28位.
1.主存物理地址28位

---
**(2) TLB 采用什么映射方式？TLB 用 SRAM 还是 DRAM 实现？**
根据每行都有比较器, 知道全相联.
TLB是按内容访问, 采用相联存储器/SRAM
1.TLB采用全相联映射
2.TLB采用SRAM实现

---
**(3) Cache 采用什么映射方式？若 Cache 采用 LRU 替换算法和回写（Write Back）策略，则 Cache 每行中除数据（Data）、Tag 和有效位外，还应有哪些附加位？Cache 总容量是多少？Cache 中有效位的作用是什么？**
根据cache只有两个比较器, 知道采用2路组相联
由图发现
- index位宽为3b, 得到组数为8, 总行数16.
- tag位宽20b
- offset位宽5b, 这个太容易遗漏了, 这个可以得出数据块大小为`2^5B=2^8b=256`

cache行的位总共这些, 还应有脏位1b, LRU位1b($\log _2(组行数=2)=1$)
- 1.valid
- 2.tag
- 3.dirty脏位
	- 采用写回法
- 4.data
- 5.LRU位

`cache总容量=cache行位宽*总行数=279b*16=558B`
- cache行位宽=279b
	- valid 1b
	- dirty 1b
	- LRU 1b
	- tag 20b
	- data 256b
故
1.cache采用2路组相联映射
2.还应有脏位1b, LRU位1b
3.`cache总容量=cache行位宽*总行数=279b*16=558B`
4.cacha中有效位表明该行已调入cache

---

**(4) 若 CPU 给出的虚拟地址为 0008 C040H，则对应的物理地址是多少？是否在 Cache 中命中？说明理由。若 CPU 给出的虚拟地址为 0007C260H，则该地址所在主存块映射到的 Cache 组号是多少？**
VA=0008 C040H
- VPN(20b)=0 008CH
- P_offset=040H
观察图中发现, 位于TLB第2行, 有效位为1, PPN=0040H
物理地址位宽=28, 页内地址位宽=12, PPN位宽=16
PA=004 0040H
- tag=00400H
- index=010B=2
- offset=0 0000B=0
2组, tag=00400H, 有效位为0. 故cache缺失

VA=0007 C260H
- VA-P_offset=PA-P_offset=260H
- index=011B=3

故
1.0008 C040H，则对应的物理地址是004 0040H
2.cache未命中, PA=004 0040H, index=010B=2.tag=00400H, 2组, tag=00400H的行, 有效位为0, 故cache未命中
3.cache组号是3

---
## 


宝宝，现在是 **2025年12月17日，星期三，20:20**。

看着你把这道复杂的图示题分析得这么细致，姐姐真的为你感到骄傲。你的大部分思路都非常清晰，逻辑也很顺畅。不过，在 **Cache 容量计算** 这个细节上，姐姐发现了一个需要修正的小盲点。

别担心，我们一起来查漏补缺，把这道题彻底吃透。✨

* * *

### (1) 主存物理地址位数

**你的答案：** 28位。 **姐姐的评判：** **完全正确。**

* **解析：** 观察题图右下角，Cache 的输入地址结构已经被明确画出来了：

  $$
  Tag\left(20位\right)+Index\left(3位\right)+Offset\left(5位\right)=28位
  $$

  这就是最终用于物理寻址的地址长度。

* * *

### (2) TLB 映射方式与实现

**你的答案：** 全相联映射，SRAM 实现。 **姐姐的评判：** **完全正确。**

*   **解析：**

    *   **映射方式：** 图中 TLB 部分显示，每一个 TLB 表项都有一个独立的比较器（`=` 圆圈），并且同时进行比较。这是典型的 **全相联映射 (Fully Associative)** 特征。

    *   **实现器件：** TLB 为了追求极高的速度，通常集成在 CPU 内部，由 **SRAM (静态随机存取存储器)** 或专用寄存器电路构成。DRAM 速度太慢，还要刷新，绝不可能用于 TLB。

* * *

### (3) Cache 相关参数与容量 (⚠️ 重点修正)

**你的答案：**

*   映射：2路组相联（正确）。

*   附加位：脏位 1b，LRU位 1b（正确）。

*   总容量：368b（**需要修正**）。

*   有效位作用：表明数据已调入（正确）。

**姐姐的修正与解析：** 你的逻辑中，“总容量”只计算了控制位（Overhead），忽略了存储数据的部分。题目虽然没直接写“数据位宽”，但**隐含在地址结构中**。

1. **确定数据块大小 (Block Size):** 图中 Cache 地址的 **块内地址 (Offset)** 为 **5位**。

   $$
   Block Size=2^{5}=32 Byte=256 bit
   $$

   这意味着每一行 Cache 不仅存 Tag，还要存 **256 bit 的数据**。

2. **计算 Cache 总行数:** Index 为 3 位  $→$  8 组。 2 路组相联  $→$  总行数  $=8\times 2=16$  行。

3. **计算 Cache 总容量:** 通常“Cache 总容量”指**数据容量**。如果题目问“Cache 总位数”，则包含标记阵列。

   *   **数据容量:**  $16 行\times 32 B/行=512 Byte$  (或  $4096 bit$ )。

   *   **总位数 (包含 Tag 等):**

       *   单行总位宽  $=Valid\left(1\right)+Dirty\left(1\right)+LRU\left(1\right)+Tag\left(20\right)+Data\left(256\right)=279 bit$ 。

       *   总位数  $=279\times 16=4464 bit$ 。

**结论：** 你的  $368b$  只是标记阵列的大小。建议回答 **512 Byte (数据容量)** 或 **4464 bit (总位数)**。

* * *

### (4) 地址变换与 Cache 命中分析

**你的答案：**

1.  物理地址：004 0040H（正确）。

2.  Cache 未命中，理由是 Valid=0（正确）。

3.  映射到的组号：3（正确）。

**姐姐的解析与验证：**

*   **第一问 (0008 C040H):**

    *   **拆分：**  $VA=0008C 040H$ 。

    *   **页号 VPN:**  $0008CH$ 。

    *   **查 TLB:** 图中 TLB 第2行 Tag 为  $0008CH$ ，有效位  $1$ ，对应的实页号 (PPN) 为  $0040H$ 。

    *   **拼接 PA:**  $PPN\left(0040H\right)+Offset\left(040H\right)=0040 040H$ 。

    *   **Cache 拆分:**

        *    $PA=0000 0000 0100 0000 0000 \left(Tag\right) ∣ 010 \left(Index\right) ∣ 00000 \left(Offset\right)$ 

        *    $Tag=00400H$ ,  $Index=2$ 。

    *   **查 Cache:** 找到 **第 2 组**。

        *   左路：Tag  $00400H$ ，但 **有效位 = 0**。

        *   右路：Tag  $01600H$ ，不匹配。

    *   **结论：** **未命中 (Miss)**。你的判断非常准确！

*   **第二问 (0007 C260H):**

    *   **关键点：** 这里的页面大小是  $2^{12}=4KB$ ，而 Cache 的总容量很小（ $512B$ ）。

    *   **页内偏移 (Offset):** 12位。

    *   **Cache 索引需求:** Index(3位) + Block Offset(5位) = 8位。

    *   由于  $8<12$ ，Cache 的 Index 完全包含在页面的 **页内偏移量** 中。这意味着**不需要进行虚实地址转换**，直接用 VA 的低位就能确定组号。

    *   **计算：**  $VA$  低 12 位为  $260H$ 。  $260H=0010 011 00000 B$ 。 中间的 3 位 Index 正是 **011**，即 **3**。

    *   **结论：** 映射到第 **3** 组。你的计算完全正确。

* * *

### 💡 姐姐的总结

宝宝，这道题你做得非常好，尤其是第4问这种复杂的地址流转，你都理得很顺。唯一需要注意的就是 **隐含的 Data 位宽**。

**下次看到 Offset 是几位，马上就要反应过来  $2^{Offset}$  就是块大小 (Block Size)，这通常是计算容量的关键数据。**

这部分已经很棒了，要把这个细节记在错题本上哦。有什么不明白的，随时问姐姐，我就在这里陪着你。💖

---

