假定计算机 M 字长为 16 位，按字节编址，连接 CPU 和主存的系统总线中地址线为 20 位、数据线为 8 位，采用 16 位定长指令字，指令格式及其说明如下：
![[2021-43-15分-指令格式.md_Attachments/2021-43-15分-指令格式-20251211194055069.png]]
**(1) ALU 的宽度是多少位？可寻址主存空间大小为多少字节？指令寄存器、主存地址寄存器（MAR）和主存数据寄存器（MDR）分别应有多少位？**
- 1.`ALU的位宽=机器字长=16位`
- 2.`可址空间大小=2^地址线数*编址空间大小=2^20*B=2MB`
- 3.`MAR位宽=地址线数=20`
- 4.`MDR位宽=数据总线宽度=8`

---
**(2) R 型格式最多可定义多少种操作？I 型和 J 型格式总共最多可定义多少种操作？通用寄存器最多有多少个？**
1.R型格式op位宽为4, 最多定义16种操作
2.I和J型格式总共最多定义`2^6=64`种操作
> 易错 I,J的OP是共享6位操作码的, 即总共定义

3.rs/rt位宽为2, 故通用寄存器最多4个

---

**(3) 假定 op1 为 0010 和 0011 时，分别表示带符号整数减法和带符号整数乘法指令，则指令 01B2H 的功能是什么（参考上述指令功能说明的格式进行描述）？若 1、2、3 号通用寄存器当前内容分别为 B052H、0008H、0020H，则分别执行指令 01B2H 和 01B3H 后，3 号通用寄存器内容各是什么？各自结果是否溢出？**
01B2H=0000 0001 1011 0010, 为R型格式指令
- op1=0010, 为带符号整数减法
- rs=01B=1, rt=10B=2, rd=11B=3
- 功能是`R[3]<-R[1]-R[2]`
01B3H=0000 0001 1011 0011, 为R型格式指令
- op1=0011, 带符号整数乘法指令
- rs=01B=1, rt=10B=2, rd=11B=3
- 功能是`R[3]<-R[1]*R[2]`

分别执行指令01B2H和01B3H
- 执行指令01B2H, `R[3]=R[1]-R[2]=B052H-0008H=B04AH`
	- **负数+正数不会溢出**
- 执行指令01B3H, `R[3]=R[1]*R[2]=B052H*0008H`
	- B052H补码-真值=-0100 1111 1010 1110
		- B052H补码-原码=1100 1111 1010 1110B=CFAEH
	- 0008H补码-真值`=8=2^3`
	- 通过移位知道, 显然溢出, 但是如何计算3号寄存器的结果呢? 目前的思路好麻烦, **应该直接对补码移位的**
	- `B052H*2^3=1011 0000 0101 0010B*2^3=1000 0010 1001 0000B=8290H`
1.01B2H 的功能是`R[3]<-R[1]-R[2]`
2.3 号通用寄存器内容各是B04AH, 8290H, 前者未溢出, 后缀溢出
> 注意这里的乘法, 实际是用的移位去处理的

---
**(4) 若采用 I 型格式的访存指令中 imm（偏移量）为带符号整数，则地址计算时应对 imm 进行零扩展还是符号扩展？**
进行符号扩展

---
**(5) 无条件转移指令可以采用上述哪种指令格式？**
按照功能描述, 选择J型


## 对话